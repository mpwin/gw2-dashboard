import React from 'react';
import { render } from '@testing-library/react';

import Collection from './Collection';

describe('Collection', () => {
  test('renders', () => {
    render(<Collection />);
  });
});
